# Stratosphere Deception Lab — landing page

The public landing page for the Stratosphere Deception Lab.

**Live site:** https://stratosphereips.github.io/deception-lab-site/

## Editing

`index.html` is a generated, self-contained bundle (all CSS, JS, fonts, and images
are inlined) exported from the authoring version in `source/`. Edit the files in
`source/` and re-export `index.html` — do not hand-patch the bundle.
